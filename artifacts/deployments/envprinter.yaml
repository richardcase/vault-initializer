apiVersion: apps/v1beta1
kind: Deployment
metadata:
  labels:
    app: envprinter
  name: envprinter
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: envprinter
      name: envprinter
    spec:
      containers:
        - name: envprinter
          image: richardcase/envprinter:0.0.1
          imagePullPolicy: Always
